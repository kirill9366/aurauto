mixin sidebar(title)
  .slider.offers__slider(
    id= title
  )
    .slider__container
      include ../card.element/element.pug
      - for(var b = 0; b < 2; b++)
        - for(var i = 1; i < 5; i++)
          .slider__item
            +card(i)
